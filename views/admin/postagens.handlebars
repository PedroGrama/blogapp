<h2>Lista de Postagens:</h2>
<hr>
<a href="/admin/postagens/add"><button class="btn btn-success ml-0.5">Nova Postagem</button></a>
{{#each postagens}}
<div class="card mt-4">
    <div>
        <h4>{{titulo}}</h4>
        <p>Descrição: {{descricao}}</p>
        <small>Data: {{data}}</small><br>
        <small>Categoria: {{categoria.nome}}</small><br>
        <a href="/admin/postagens/edit/{{_id}}"><button class="btn btn-success mt-3">Editar Postagem</button></a>
        <form action="/admin/postagens/deletar/{{_id}}" method="POST" style="display: inline;">
            <button type="submit" class="btn btn-danger mt-3">Excluir Postagem</button>
        </form>
    </div>
</div>
{{else}}
<p>Nenhuma postagem registrada</p>
{{/each}}